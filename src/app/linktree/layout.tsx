export const metadata = {
  title: "Links | Escovato",
  description: "Todos os nossos links em um só lugar",
};

export default function LinktreeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
